import { FeaturedProducts, Hero, Services, Contacts } from '../../components';

const HomePage = () => {
    return <main>
        <Hero />
        <FeaturedProducts />
        <Services />
        <Contacts />
    </main>
}

export default HomePage;
